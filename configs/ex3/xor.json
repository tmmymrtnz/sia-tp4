{
    "layer_sizes": [2, 2, 1],
    "activations": ["", "tanh", "step"],
    "loss": "mse",
    "optimizer": "sgd",
    "optim_kwargs": {
      "learning_rate": 0.01
    },
    "batch_size": 4,
    "max_epochs": 100
  }